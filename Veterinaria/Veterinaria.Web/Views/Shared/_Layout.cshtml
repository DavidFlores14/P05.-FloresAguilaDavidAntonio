<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css" rel="stylesheet" />
    

</head>
<body>
    <nav class="navbar navbar-expand-lg text-white navbar-light bg-dark">
        <a class="navbar-brand text-white" href="@Url.Action("Index", "Home")">Veterinaria</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link text-white" href="@Url.Action("Index", "Principal")">Inicio</a>
                </li>

                <li class="nav-item active">
                    <a class="nav-link text-white" href="@Url.Action("About", "Home")">Acerca</a>
                </li>

                <li class="nav-item active">
                    <a class="nav-link text-white" href="@Url.Action("Contact", "Home")">Contacto</a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Usuarios
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="@Url.Action("Login", "Account")">Iniciar sesión</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="@Url.Action("Register", "Account")">Registrar</a>
                    </div>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="search" placeholder="Buscar" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Buscar</button>
            </form>

            @*
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Servicios
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="@Url.Action("Index", "Veterinary")">Veterinario</a>
                            <a class="dropdown-item" href="@Url.Action("Index", "Consults")">Consultas</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="" @Url.Action("Index", "Pets")>Mascotas</a>
                        </div>
                    </li>
                </ul>
                <form class="form-inline my-2 my-lg-0">
                    <input class="form-control mr-sm-2" type="search" placeholder="Buscar" aria-label="Search">
                    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Buscar</button>
                </form>
            </div>
        </nav>*@

            @*<div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link text-white" href="@Url.Action("Index", "Principal")">Inicio</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="@Url.Action("About", "Home")">Acerca</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="@Url.Action("Contact", "Home")">Contacto</a>
            </li>
            *@


            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item">
                    <a class="nav-link text-white" href="@Url.Action("Index", "Users")">Usuarios</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="@Url.Action("Index", "Veterinary")">Medico</a>
                </li>
            }

            @if (User.IsInRole("Veterinary"))
            {
                <li class="nav-item">
                    <a class="nav-link text-white" href="@Url.Action("Index", "Consults")">Consultas</a>
                </li>
            }

            @if (User.IsInRole("Owner"))
            {
                <li class="nav-item">
                    <a class="nav-link text-white" href="@Url.Action("Index","Pets")">Mascotas</a>
                </li>

            }

            </ul>


            <span class="navbar-text">
                @Html.Partial("_LoginPartial")
            </span>

        </div>
    </nav>
    <div class="">
        @RenderBody()
        <hr />
        <footer>
            <p class="text-center">&copy;Todos los derechos reservados @DateTime.Now.Year - Flores Águila David Antonio <br />Veterinaria ♥</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
